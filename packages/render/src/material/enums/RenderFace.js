
/**
 * Set which face for render.
 * @enum {number}
 */
export const RenderFace = {
  /** Render front face. */
  Front: 0,
  /** Render back face. */
  Back: 1,
  /** Render double face. */
  Double: 2
};
